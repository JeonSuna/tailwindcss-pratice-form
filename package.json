{
  "scripts": {
    "dev": "tailwindcss -i ./src/styles/input.css -o ./src/styles/output.css",
    "start": "run-p watch serve",
    "watch": "npm run dev -- --watch",
    "serve": "live-server --open=/src/"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.4.10"
  }
}
